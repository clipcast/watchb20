# Chain ID for Base Sepolia testnet
NEXT_PUBLIC_CHAIN_ID=
# JSON-RPC endpoint for Base Sepolia
NEXT_PUBLIC_RPC_URL=
# Block explorer base URL (used for tx/address links)
NEXT_PUBLIC_EXPLORER_URL=
# Base Notifications REST API key (server-side only, never expose)
BASE_NOTIFICATIONS_API_KEY=
# Public app URL (used by Base Notifications targeting)
NEXT_PUBLIC_APP_URL=
